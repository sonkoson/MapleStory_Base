function act() {
	if (rm.getMap().getId() == 105200410) {
		rm.spawnMonster(8930100, -192, 443, 3, 3000);
		rm.startMapEffect("เป็นเจ้าเองสินะที่เมินเฉยต่อคำเตือนของข้าแล้วกลับมาอีก ข้าจะไม่ปรานีอีกต่อไป", 5120103, 3);
	}
}